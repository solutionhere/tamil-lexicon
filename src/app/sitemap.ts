import { MetadataRoute } from 'next';

/**
 * Sitemap generation has been disabled.
 * This file is kept to prevent build errors if a sitemap is expected but can be removed if not needed.
 */
export default function sitemap(): MetadataRoute.Sitemap {
  return [];
}
